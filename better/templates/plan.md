# [기능명] 구현 계획 — #이슈번호

> spec.md를 기반으로 작성. 코드 수준의 구현 방법과 결정 사항을 정리한다.
> 기획이 변경되면 spec.md를 먼저 수정하고, 이 파일의 변경 이력 섹션에 사유를 기록한다.

---

## 브랜치

`feature/#이슈번호-기능명`

---

## 파일 구조

### 신규 생성

```
src/
├── features/[기능명]/
│   ├── types.ts          # 타입 정의
│   ├── [화면].tsx
│   └── ...
└── pages/dashboard/[경로]/
    ├── [화면].tsx
    └── ...
```

### 수정되는 파일

| 파일 | 수정 내용 |
|------|-----------|
| `src/routes/paths.ts` | 경로 상수 추가 |
| `src/routes/dashboard/dashboardRoutes.tsx` | 라우트 등록 |
| `src/pages/.../[진입점].tsx` | 진입 로직 추가 |

### 삭제되는 파일

| 파일 | 이유 |
|------|------|
| `src/...` | [삭제 이유] |

---

## 라우트 경로

```
/dashboard/[기능]/[화면1]
/dashboard/[기능]/[화면2]   ← 쿼리파라미터 있으면 명시 (예: ?step=STEP_ID)
```

경로 상수: `PATH_DASHBOARD.[기능명].*` (`src/routes/paths.ts`)

---

## 데이터 모델

### 폼 데이터 / 주요 타입

```
필드명      타입      설명
------      ----      ----
...         ...       ...
```

### localStorage

- 키: `[storage_key]`
- 저장 시점: [언제]
- 삭제 시점: [언제]
- 저장 범위: [어떤 값이 포함되고 포함되지 않는지]

---

## 핵심 구현 사항

### 1. [구현 주제]

[왜 이 방식을 선택했는지 한 줄]

```typescript
// 구현 예시
```

| 상황 | 동작 |
|------|------|
| ... | ... |

### 2. [구현 주제]

...

---

## 미구현 / API 연동 대기

| 항목 | 현재 상태 | 연동 시 작업 위치 |
|------|-----------|------------------|
| [항목] | 임시 처리 / 미연동 | `파일명.tsx` |

---

## 코딩 컨벤션 (이 기능에서 확인된 것)

- 한국어 주석
- Props 인터페이스: `IProps`
- Boolean 변수: `isXXX`, `hasXXX`
- 경로 상수: `PATH_DASHBOARD.*` (하드코딩 금지)
- [이 기능에서 추가로 사용된 컴포넌트/패턴]

---

## 변경 이력

| 날짜 | 변경 내용 | 관련 파일 |
|------|-----------|-----------|
| YYYY-MM-DD | 최초 작성 | 전체 |
